const student = {
    sName: "ashik",
    sAge: 30,
    sGender: "male"
}
const objKeys = Object.keys(student)
console.log("objKey", objKeys)

const objValues = Object.values(student)
console.log("objValues", objValues)

const objLength = Object.values(student).length
console.log("objLength", objLength)